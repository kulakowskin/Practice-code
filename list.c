#include <stdio.h>

int main()
{
      printf("Please enter number");
      scanf("%d", %entries);

      int numbers[entries];
      int newArr[entries];

      printf("Please enter set of %d entries: ", entries);

      for (int i = 0; i< entries; i++)
      {
            scanf("%d",&numbers[i]);
      }

      for (int i = 0; ;numbers[i+1])
      {

      }
}
